<UserControl x:Class="WinSetup.Pro.UI.Views.ProfilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel MaxWidth="820" HorizontalAlignment="Left">
        <TextBlock Style="{StaticResource H1}" Text="Choose a profile" />
        <TextBlock Style="{StaticResource Muted}"
                   Text="A profile is a curated set of components. You can still customise it on the next screen." />

        <ListBox ItemsSource="{Binding Profiles}" SelectedItem="{Binding Selected}"
                 Margin="0,14,0,0" Background="Transparent" BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 HorizontalContentAlignment="Stretch">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Margin" Value="0,0,0,8" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="b" Background="{DynamicResource Card}"
                                        BorderBrush="{DynamicResource Border}" BorderThickness="1"
                                        CornerRadius="8" Padding="16" Cursor="Hand">
                                    <ContentPresenter />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="b" Property="BorderBrush" Value="{DynamicResource Accent}" />
                                        <Setter TargetName="b" Property="BorderThickness" Value="2" />
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="b" Property="Opacity" Value="0.95" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <DockPanel>
                            <TextBlock Text="{Binding Name}" FontFamily="Segoe UI Semibold" FontSize="15" />
                            <TextBlock DockPanel.Dock="Right" HorizontalAlignment="Right"
                                       Style="{StaticResource Muted}" FontSize="12">
                                <Run Text="{Binding ComponentCount, Mode=OneWay}" /><Run Text=" components" />
                            </TextBlock>
                        </DockPanel>
                        <TextBlock Style="{StaticResource Muted}" Text="{Binding Description}" Margin="0,4,0,0" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
            <Button Content="Back" Command="{Binding BackCommand}" />
            <Button Style="{StaticResource Primary}" Content="Continue" Margin="8,0,0,0"
                    Command="{Binding ContinueCommand}" />
        </StackPanel>
    </StackPanel>
</UserControl>
