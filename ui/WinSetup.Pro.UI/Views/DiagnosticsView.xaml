<UserControl x:Class="WinSetup.Pro.UI.Views.DiagnosticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel MaxWidth="760" HorizontalAlignment="Left">
        <DockPanel>
            <TextBlock Style="{StaticResource H1}" Text="System diagnostics" />
            <Button DockPanel.Dock="Right" HorizontalAlignment="Right" Content="Refresh"
                    Command="{Binding RefreshCommand}" />
        </DockPanel>

        <Border Style="{StaticResource Card}" Margin="0,12,0,0">
            <ItemsControl ItemsSource="{Binding Checks}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="2,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="22" />
                                <ColumnDefinition Width="220" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="●" Foreground="{Binding Ok, Converter={StaticResource BoolToBrush}}" />
                            <TextBlock Grid.Column="1" Text="{Binding Name}" />
                            <TextBlock Grid.Column="2" Text="{Binding Detail}" Style="{StaticResource Muted}" FontSize="12" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>

        <TextBlock Style="{StaticResource Muted}" Margin="0,8,0,0">
            <Run Text="{Binding Failing, Mode=OneWay}" /><Run Text=" check(s) need attention." />
        </TextBlock>
    </StackPanel>
</UserControl>
